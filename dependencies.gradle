ext {
    kotlinVersion = '1.4.32'
    gradleVersion = '4.1.3'

    // Google Service
    googleServiceVersion = '4.3.8'

    // Crashlytics
    crashlyticsVersion = '2.7.0'

    // Android Basic
    appCompatVersion = '1.3.0'
    materialVirsion = '1.3.0'
    constraintLayoutVersion = '2.0.4'
    recyclerViewVersion = '1.2.1'

    // KTX
    coreKtxVersion = '1.5.0'
    fragmentKtxVersion = '1.3.4'

    // DataStore
    dataStoreVersion = '1.0.0-alpha07'

    // Protobuf
    protobufPluginVersion = '0.8.12'
    protobufJavaliteVersion = '3.10.0'

    // Networking lib
    okhttpVersion = '4.2.2'
    retrofitVersion = '2.9.0'
    chuckerVersion = '3.4.0'

    // RxJava
    rxJavaVersion = '3.0.12'
    rxAndroidVersion = '3.0.0'
    rxKotlinVersion = '3.0.1'

    // Glide
    glideVersion = '4.12.0'

    // Dagger Hilt
    hiltCoreVersion = '2.35.1'

    // Chrome Custom Tabs
    chromeCustomTabsVersion = '1.3.0'

    // Firebase
    firebaseVersion = '28.1.0'

    // Camera
    cameraVersion = '1.1.0-alpha06'
    cameraViewVersion = '1.0.0-alpha26'

    // DiscreteScrollView
    discreteScrollViewVersion = '1.5.1'

    // Test
    junitVersion = '4.13.2'
    androidJunitVersion = '1.1.2'
    espressoVersion = '3.3.0'
}